/**
 * CGN-010 shim — behavior lives in js/live-stubs.js (HTML Coder shell hooks).
 * Kept so script order / older notes still resolve.
 */
(function () {
  "use strict";
  /* live-stubs.js owns Go live */
})();
